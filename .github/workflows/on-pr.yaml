name: on-pr

on:
  pull_request:
    types: [labeled, opened, reopened, synchronize]
    paths:
      - ".github/workflows/on-pr.yaml"
      - ".gitops/**"
      - "apis/**"
      - "examples/**"
      - "tests/**"
      - "functions/**"
      - "upbound.yaml"

permissions:
  packages: write
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  setup:
    uses: unbounded-tech/workflows-crossplane/.github/workflows/setup.yaml@v2.19.1

  validate:
    needs: setup
    uses: unbounded-tech/workflows-crossplane/.github/workflows/validate.yaml@v2.19.1
    with:
      examples: |
        [
          { "example": "examples/autoeksclusters/minimal.yaml" },
          { "example": "examples/autoeksclusters/standard.yaml" },
          { "example": "examples/autoeksclusters/with-network.yaml" },
          { "example": "examples/autoeksclusters/import.yaml" },
          { "example": "examples/autoeksclusters/test/example.yaml", "observed_resources": "examples/autoeksclusters/test/mocks/observed-resources/steps/1" },
          { "example": "examples/autoeksclusters/test/example.yaml", "observed_resources": "examples/autoeksclusters/test/mocks/observed-resources/steps/2" },
          { "example": "examples/autoeksclusters/test/example.yaml", "observed_resources": "examples/autoeksclusters/test/mocks/observed-resources/steps/3" },
          { "example": "examples/autoeksclusters/test/example.yaml", "observed_resources": "examples/autoeksclusters/test/mocks/observed-resources/steps/4" },
          { "example": "examples/autoeksclusters/test/example.yaml", "observed_resources": "examples/autoeksclusters/test/mocks/observed-resources/steps/5" },
          { "example": "examples/autoeksclusters/with-network/test/example.yaml", "observed_resources": "examples/autoeksclusters/with-network/test/mocks/observed-resources/steps/1" }
        ]
      api_path: apis/autoeksclusters
      error_on_missing_schemas: true

  test:
    needs: setup
    uses: unbounded-tech/workflows-crossplane/.github/workflows/test.yaml@v2.19.1

  e2e:
    needs: setup
    uses: unbounded-tech/workflows-crossplane/.github/workflows/e2e.yaml@v2.19.1
    with:
      aws: true
      aws-use-oidc: true
      aws-account-id: "${{ vars.AWS_ACCOUNT_ID }}"
      aws-region: "${{ vars.AWS_REGION }}"
      timeout-minutes: 90
      cleanup-timeout-minutes: 90
      write-env-files: true
      env-vars: |
        {
          "ADMIN_ROLE_ARN": "${{ vars.ADMIN_ROLE_ARN }}",
          "PRIVATE_SUBNET_ID_A": "${{ vars.PRIVATE_SUBNET_ID_A }}",
          "PRIVATE_SUBNET_ID_B": "${{ vars.PRIVATE_SUBNET_ID_B }}"
        }

  publish:
    needs:
      - validate
      - test
    uses: unbounded-tech/workflows-crossplane/.github/workflows/publish.yaml@v2.19.1
    secrets: inherit
    with:
      tag: pr-${{ github.event.pull_request.number }}-${{ github.sha }}
